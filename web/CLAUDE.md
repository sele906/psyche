# psyche-web

React (Vite). 카카오톡 스타일 채팅 UI.

## 실행

npm run dev     # 5173

## 규칙

- `.env`는 권한 설정으로 접근이 차단되어 있다.
필요한 환경변수 목록은 `.env.example`에 있으니 그쪽을 참고할 것.
- 메신저 UI이므로 실제 메신저의 동작 관례를 따른다
  (읽음 표시, 입력 중 인디케이터, 날짜 구분선, 말풍선 꼬리)